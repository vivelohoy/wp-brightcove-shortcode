<!-- Start of Brightcove Player -->
<div style="display:none"></div>
<!--
By use of this code snippet, I agree to the Brightcove Publisher T and C 
found at https://accounts.brightcove.com/en/terms-and-conditions/. 
-->
<script language="JavaScript" type="text/javascript" src="http://admin.brightcove.com/js/BrightcoveExperiences.js"></script>
<!-- End of Brightcove Player -->
<object id="myExperience{{ VIDEO_ID }}" class="BrightcoveExperience">
  <param name="bgcolor" value="#FFFFFF" />
  <param name="width" value="{{ VIDEO_WIDTH }}" />
  <param name="height" value="{{ VIDEO_HEIGHT }}" />
  <param name="playerID" value="{{ PLAYER_ID }}" />
  <param name="playerKey" value="{{ PLAYER_KEY }}" />
  <param name="isVid" value="true" />
  <param name="isUI" value="true" />
  <param name="dynamicStreaming" value="true" />
  <param name="@videoPlayer" value="{{ VIDEO_ID }}" />
  <param name="linkBaseURL" value="http://vivelohoy.com/" />
  <param name="adServerURL" value="http://pubads.g.doubleclick.net/gampad/ads?iu=/4011/trb.vivelohoy2/video&sz=3x3v&cust_params=pos%3Dpre&impl=s&gdfp_req=1&env=vp&output=xml_vast2&unviewed_position_start=1&correlator=" />
</object>
<!-- 
This script tag will cause the Brightcove Players defined above it to be created as soon
as the line is read by the browser. If you wish to have the player instantiated only after
the rest of the HTML is processed and the page load is complete, remove the line.
-->
<script type="text/javascript">
    document.querySelector('param[name=linkBaseURL]').value = document.location.href;
    correlator = Math.round( Math.random() * 1000000000 );
    adServerURL_base = document.querySelector("object.BrightcoveExperience param[name=adServerURL]").value;
    adServerURL = adServerURL_base + correlator;
    document.querySelector("object.BrightcoveExperience param[name=adServerURL]").value = adServerURL;
    brightcove.createExperiences();
</script>
<!-- End of Brightcove Player -->
